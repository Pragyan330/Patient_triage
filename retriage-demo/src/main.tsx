import React from 'react'
import ReactDOM from 'react-dom/client'
import RetriageQueue from './RetriageQueue'
import './index.css'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <RetriageQueue />
  </React.StrictMode>,
)
